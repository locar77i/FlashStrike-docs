title: FlashStrike Documentation
description: High-performance Matching Engine Architecture
theme: just-the-docs

# Build settings
markdown: kramdown

# Where pages live
collections:
  docs:
    permalink: "/:path/"
    output: true

just_the_docs:
  # Enable navigation on the left
  collections:
    docs:
      name: Documentation
      nav_exclude: false

# Optional: Search
search_enabled: true

