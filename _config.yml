title: FlashStrike Documentation
description: High-performance Matching Engine Architecture

# Use Just the Docs via GitHub Pages
remote_theme: just-the-docs/just-the-docs

# Optional: Configure Markdown & navigation
markdown: kramdown

search_enabled: true

# Collections (your docs folder)
collections:
  docs:
    output: true
    permalink: "/:collection/:path/"

just_the_docs:
  collections:
    docs:
      name: Documentation

